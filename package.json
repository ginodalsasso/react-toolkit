{
  "name": "react-toolkit",
  "version": "1.0.0",
  "description": "a simple react and js toolkit",
  "private": true,
  "workspaces": ["cli"],
  "scripts": {
    "dev": "npm run dev --workspace=cli",
    "build": "npm run build --workspace=cli"
  }
}
